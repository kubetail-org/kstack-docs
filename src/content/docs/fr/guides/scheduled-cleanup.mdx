---
title: "Nettoyage planifié avec --older-than"
description: "Exécuter /cleanup via cron pour supprimer les ressources kstack obsolètes."
---

:::note
Page provisoire. Ce contenu couvrira l'exécution de `/cleanup --older-than 24h --yes` depuis un job cron/CI, le ciblage avec `--skill` et `--session`, et le maintien d'un cluster propre sur de nombreuses sessions d'agent.
:::
