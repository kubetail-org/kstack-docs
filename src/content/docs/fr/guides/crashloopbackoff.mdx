---
title: Déboguer un pod CrashLoopBackOff
description: "Parcourir le diagnostic d'un pod en boucle de crash avec /investigate, /logs et /exec."
---

:::note
Page provisoire. Ce contenu présentera une session de débogage réaliste combinant `/investigate`, `/logs --grep` et `/exec --copy-to` sur un pod en boucle de crash.
:::
