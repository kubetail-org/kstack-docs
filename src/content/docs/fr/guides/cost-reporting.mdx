---
title: Rapports de coûts par namespace/équipe
description: "Produire des rapports de coûts par équipe avec /audit-cost et OpenCost."
---

:::note
Page provisoire. Ce contenu couvrira l'utilisation de `/audit-cost` avec `--namespace` pour des rapports au niveau de l'équipe, l'utilisation de `--window` pour des fenêtres rétrospectives plus longues, et le filtrage du bruit avec `--min-savings` lorsque OpenCost est installé.
:::
