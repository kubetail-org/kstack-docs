---
title: Depuración de un pod en CrashLoopBackOff
description: Proceso paso a paso para diagnosticar un pod con crash loop usando /investigate, /logs y /exec.
---

:::note
Página de marcador de posición. Cubrirá una sesión de depuración realista que combina `/investigate`, `/logs --grep` y `/exec --copy-to` contra un pod en crash loop.
:::
