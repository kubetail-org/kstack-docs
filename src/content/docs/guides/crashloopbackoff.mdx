---
title: Debugging a CrashLoopBackOff pod
description: Walk through diagnosing a crash-looping pod using /investigate, /logs, and /exec.
---

:::note
Placeholder page. This will walk through a realistic debugging session combining `/investigate`, `/logs --grep`, and `/exec --copy-to` against a crash-looping pod.
:::
