---
title: Cost reporting per namespace/team
description: Produce per-team cost reports with /audit-cost and OpenCost.
---

:::note
Placeholder page. This will cover scoping `/audit-cost` with `--namespace` for team-level reports, using `--window` for longer lookbacks, and filtering out noise with `--min-savings` when OpenCost is installed.
:::
