---
title: Scheduled cleanup with --older-than
description: Run /cleanup on a cron to garbage-collect stale kstack-owned resources.
---

:::note
Placeholder page. This will cover running `/cleanup --older-than 24h --yes` from a cron/CI job, scoping with `--skill` and `--session`, and keeping the cluster tidy across many agent sessions.
:::
