---
title: 按命名空间/团队划分的成本报告
description: 使用 /audit-cost 和 OpenCost 生成团队级成本报告。
---

:::note
占位页面。将涵盖以下内容：使用 `--namespace` 将 `/audit-cost` 限定到团队级别报告、使用 `--window` 进行较长时间回溯，以及在安装 OpenCost 时使用 `--min-savings` 过滤噪声。
:::
