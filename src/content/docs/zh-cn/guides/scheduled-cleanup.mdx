---
title: 使用 --older-than 定时清理
description: 通过 cron 运行 /cleanup，对 kstack 拥有的过期资源进行垃圾回收。
---

:::note
占位页面。将涵盖以下内容：从 cron/CI Job 运行 `/cleanup --older-than 24h --yes`、使用 `--skill` 和 `--session` 限定清理范围，以及跨多个代理会话保持集群整洁。
:::
