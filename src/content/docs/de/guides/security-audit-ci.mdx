---
title: Security-Audit in CI ausführen (SARIF)
description: SARIF-Ausgabe von /audit-security für GitHub Code Scanning und andere CI-Integrationen emittieren.
---

:::note
Platzhalterseite. Diese deckt ab, wie `/audit-security --format sarif` in eine CI-Pipeline integriert wird, wie `--standard` angepasst wird und wie Findings als GitHub-Code-Scanning-Alerts importiert werden.
:::
