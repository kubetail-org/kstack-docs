---
title: Debugging eines CrashLoopBackOff-Pods
description: Diagnose eines crash-loopenden Pods mit /investigate, /logs und /exec — Schritt für Schritt.
---

:::note
Platzhalterseite. Diese führt durch eine realistische Debugging-Session mit `/investigate`, `/logs --grep` und `/exec --copy-to` gegen einen crash-loopenden Pod.
:::
