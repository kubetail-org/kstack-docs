---
title: '--older-than によるスケジュールされたクリーンアップ'
description: 'cron で /cleanup を実行して、kstack所有の古いリソースをガベージコレクションします。'
---

:::note
プレースホルダーページ。ここでは、cron/CIジョブから `/cleanup --older-than 24h --yes` を実行すること、`--skill` と `--session` でスコープを設定すること、および多数のエージェントセッションにわたってクラスターをきれいに保つことについて説明します。
:::
