---
title: Limpeza agendada com --older-than
description: Execute /cleanup em um cron para remover recursos obsoletos gerenciados pelo kstack.
---

:::note
Página provisória. Esta página cobrirá a execução de `/cleanup --older-than 24h --yes` a partir de um job cron/CI, o escopo com `--skill` e `--session`, e como manter o cluster organizado ao longo de muitas sessões de agent.
:::
