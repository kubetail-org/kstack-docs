---
title: Depurando um pod CrashLoopBackOff
description: Passo a passo para diagnosticar um pod em crash-loop usando /investigate, /logs e /exec.
---

:::note
Página provisória. Esta página percorrerá uma sessão de depuração realista combinando `/investigate`, `/logs --grep` e `/exec --copy-to` em um pod com crash-loop.
:::
